; Field-service OS photos are uploaded full-resolution to keep their EXIF (GPS +
; date/time = proof of on-site work), so the defaults (2M/8M) are too small.
upload_max_filesize = 30M
post_max_size = 35M
max_file_uploads = 30
