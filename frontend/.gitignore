# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
*.tsbuildinfo

# native — generated by `expo prebuild` (managed workflow). ViroReact ships an
# Expo config plugin from 2.57, so the AR native setup regenerates from app.json
# and no longer needs hand-editing (which is why this used to be versioned).
ios/
android/

# env
.env
.env.local

# misc
.DS_Store
*.log
