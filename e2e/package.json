{
  "name": "walvee-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests (Playwright) for the walvee customer + provider web apps.",
  "scripts": {
    "test": "playwright test",
    "test:customer": "playwright test --project=customer",
    "test:provider": "playwright test --project=provider",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
